{
  "name": "@f/debounce",
  "description": "Debounce a function",
  "repository": "git://github.com/micro-js/debounce.git",
  "version": "1.0.5",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
